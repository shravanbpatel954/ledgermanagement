{
  "name": "mu-project-1",
  "private": true,
  "description": "EDIMS — run API + React together",
  "scripts": {
    "dev": "concurrently -n api,web -c blue,magenta \"npm run dev --prefix edims-backend\" \"npm start --prefix client\"",
    "start": "npm run dev"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
